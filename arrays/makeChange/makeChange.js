function makeChange (arr, x) {
  var count = 0;
  var result = [];

  while (count <= x) {
    for (var i = 0; i < arr.length; i++) {
      count +=
    }
  }
}

console.log(makeChange([25, 10, 5, 1], 16))