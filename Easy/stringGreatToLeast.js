function greatToLeast(str) {
  var countObj = {};
  var result = [];
  //loop through string
    //if character exists in string
      //increment
    //if not, add key value pair
  //sort objects values by greatest value to least value
  for (var i = 0; i < str.length; i++) {
    if (countObj[str[i]] === )
  }
}

console.log(greatToLeast('aabbbc'))